<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color (red theme). -->
        <item name="colorPrimary">#D32F2F</item> <!-- Red color -->
        <item name="colorPrimaryVariant">#B71C1C</item> <!-- Darker red for the variant -->
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Status bar color (match the primary color variant). -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- ActionBar customization -->
        <item name="android:actionBarStyle">@style/CustomActionBarStyle</item>
        <item name="android:windowBackground">#D32F2F</item> <!-- Black background for night mode -->

        <!-- Customize your theme here -->
    </style>

    <!-- Custom ActionBar style -->
    <style name="CustomActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:titleTextColor">#FFFFFF</item> <!-- White title text -->
        <item name="android:background">#D32F2F</item> <!-- Red background for ActionBar -->
    </style>
</resources>
